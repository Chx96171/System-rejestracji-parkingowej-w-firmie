@{
    ViewData["Title"] = "Demonstracja Wzorców Projektowych";
}

<div class="container mt-4">
    <h1 class="mb-4">Demonstracja Wzorców Projektowych</h1>
    <p class="lead">System rejestracji parkingowej implementuje 6 wzorców projektowych:</p>

    <div class="row mt-4">
        <div class="col-md-6">
            <div class="card mb-3">
                <div class="card-header bg-primary text-white">
                    <h5 class="mb-0">1. Simple Factory</h5>
                </div>
                <div class="card-body">
                    <p>Tworzenie różnych typów pojazdów (Car, Motorcycle, Truck, Bus)</p>
                    <a href="@Url.Action("SimpleFactory")" class="btn btn-primary btn-sm">Zobacz Demo</a>
                </div>
            </div>
        </div>

        <div class="col-md-6">
            <div class="card mb-3">
                <div class="card-header bg-success text-white">
                    <h5 class="mb-0">2. Factory Method</h5>
                </div>
                <div class="card-body">
                    <p>Tworzenie różnych typów miejsc parkingowych (Standard, VIP, Disabled, Electric)</p>
                    <a href="@Url.Action("FactoryMethod")" class="btn btn-success btn-sm">Zobacz Demo</a>
                </div>
            </div>
        </div>

        <div class="col-md-6">
            <div class="card mb-3">
                <div class="card-header bg-info text-white">
                    <h5 class="mb-0">3. Abstract Factory</h5>
                </div>
                <div class="card-body">
                    <p>Tworzenie kompletnych zestawów obiektów dla stref parkingowych (Public, Employee, VIP)</p>
                    <a href="@Url.Action("AbstractFactory")" class="btn btn-info btn-sm">Zobacz Demo</a>
                </div>
            </div>
        </div>

        <div class="col-md-6">
            <div class="card mb-3">
                <div class="card-header bg-warning text-dark">
                    <h5 class="mb-0">4. Singleton</h5>
                </div>
                <div class="card-body">
                    <p>Globalna konfiguracja systemu parkingowego (thread-safe)</p>
                    <a href="@Url.Action("Singleton")" class="btn btn-warning btn-sm">Zobacz Demo</a>
                </div>
            </div>
        </div>

        <div class="col-md-6">
            <div class="card mb-3">
                <div class="card-header bg-danger text-white">
                    <h5 class="mb-0">5. Mediator</h5>
                </div>
                <div class="card-body">
                    <p>Koordynacja komunikacji między rezerwacjami, miejscami i użytkownikami</p>
                    <a href="@Url.Action("Mediator")" class="btn btn-danger btn-sm">Zobacz Demo</a>
                </div>
            </div>
        </div>

        <div class="col-md-6">
            <div class="card mb-3">
                <div class="card-header bg-secondary text-white">
                    <h5 class="mb-0">6. Interpreter</h5>
                </div>
                <div class="card-body">
                    <p>Interpretacja i walidacja reguł parkingowych (WorkersOnly, VIPAccess, NoTrucks)</p>
                    <a href="@Url.Action("Interpreter")" class="btn btn-secondary btn-sm">Zobacz Demo</a>
                </div>
            </div>
        </div>
    </div>

    <div class="mt-4">
        <a href="@Url.Action("Summary")" class="btn btn-dark btn-lg">Podsumowanie Wszystkich Wzorców</a>
    </div>

    <div class="mt-5">
        <h3>Struktura Projektu</h3>
        <ul class="list-group">
            <li class="list-group-item"><strong>/Factories</strong> - Simple Factory, Factory Method, Abstract Factory</li>
            <li class="list-group-item"><strong>/Models/Vehicles</strong> - Klasy bazowe i konkretne typy pojazdów</li>
            <li class="list-group-item"><strong>/Models/ParkingSpots</strong> - Klasy miejsc parkingowych</li>
            <li class="list-group-item"><strong>/Configuration</strong> - Singleton dla konfiguracji</li>
            <li class="list-group-item"><strong>/Mediators</strong> - Mediator Pattern</li>
            <li class="list-group-item"><strong>/Interpreters</strong> - Interpreter Pattern</li>
            <li class="list-group-item"><strong>/Services</strong> - Serwisy wykorzystujące wzorce</li>
        </ul>
    </div>
</div>
